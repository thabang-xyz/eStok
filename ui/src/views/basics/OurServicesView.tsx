import PublicLayout from '../../layout/PublicLayout';
import OurServiceComponent from '../../components/basics/OurServiceComponent';

const OurServicesView = () => {
  return (
    <PublicLayout>
      <OurServiceComponent />
    </PublicLayout>
  );
};

export default OurServicesView;
